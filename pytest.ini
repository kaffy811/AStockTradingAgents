[pytest]
asyncio_mode = auto
pythonpath = backend
testpaths = backend/tests
norecursedirs =
    .git
    .venv
    backend/.venv
    frontend
    node_modules
    dist
    build
    .clone
    .claude
    HKNA
filterwarnings =
    default
